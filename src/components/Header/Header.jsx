import React from 'react';
import { Navbar } from "../index";

const Header = () => {
    return (
        <header id='header' className='fixed top-0 w-100 z-10'>
            <Navbar />
        </header>
    )
}

export default Header
